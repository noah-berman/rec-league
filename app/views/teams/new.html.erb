<h2> Create a team </h2>

<%= form_with model: @team do |f| %>

  <%= f.label :team_name %>
  <%= f.text_field :team_name %> <br>

  Select a league <%= f.collection_select :league_id, League.all, :id, :league_name %>

  <%= f.submit "Create team" %>

<% end %>
